# How to run:

1. Download `uv python package manager`
2. Run:

```python
    uv sync
```
This will download all the dependencies needed for the projects to run.

3. To run a project:
```python
    uv run projects/project_name.py
```